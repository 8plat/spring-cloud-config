server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: git@gitee.com:plat8/config-repo.git
          search-paths:
            - config
          username: 871808065@qq.com
          password: powerdata?2015
      label:  master

eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka
